const soma = (n1 , n2) => n1 + n2

console.log(soma(100,10))


const names = [
    'gabriel',
    'joao',
    'murillo',
    'pedro'
];

const resNames = names.map(names => names.length) 

console.log(resNames)